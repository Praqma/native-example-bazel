sudo: required

services:
  - docker

language: cpp

compiler:
  - clang
  - gcc

notifications:
  email:
    on_success: never # default: change
    on_failure: change # default: always

before_install:
  - docker pull praqma/native-bazel:latest

script:
  - cat /etc/*-release
  - clang --version
  - gcc --version
  - java --version
  - dot -V

script:
  - ls -al ${PWD}
  - docker run --rm --workdir=/workspace --volume ${PWD}:/workspace dranoel/cbs-devel:latest ./build.sh
  - docker run --rm --workdir=/workspace --volume ${PWD}:/workspace dranoel/cbs-devel:latest ./test.sh